# Exports
source ~/.exports

# Imports
source ~/.imports

# Evals
source ~/.evals

# Enable git completion
if [ -f ~/.git-completion.bash ]
then
    . ~/.git-completion.bash
fi

# Enable npm completion
if [ -f ~/.npm-completion ]
then
    . ~/.npm-completion
fi

# Enable ng completion
if [ -f ~/.ng-completion ]
then
    . ~/.ng-completion
fi
